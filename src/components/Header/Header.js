import React from 'react';
import classes from "./Header.module.scss";

const Header = () => {
    return (
        <>
            <div className={classes.Header}>

            </div>
            <div className={classes.Header + " " + classes.phantom}>

            </div>
        </>
    );
};

export default Header;